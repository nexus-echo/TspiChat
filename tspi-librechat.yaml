version: 1.3.15

cache: true

interface:
  agents:
    use: true
    create: true
    share: false
    public: false
  webSearch: true

webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
